<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>fOLDER</title>
  </head>
  <body>
    <% if (files && files.length > 0) { %>
    <h2>Files</h2>
    <div class="grid">
      <% files.forEach(function(file) { %>
      <a href="/Adrive/<%= userId %>/<%= file.id %>" class="file">
        📁 <br />
        <%= file.name %>
      </a>
      <% }); %>
    </div>
    <% } %>
  </body>
</html>
